services:
  thelounge:
    build: .
    container_name: thelounge-fork
    ports:
      - "9000:9000"
    restart: always
    volumes:
      - thelounge:/var/opt/thelounge # uses a named volume

volumes:
  thelounge:
